/**
 * Left number column for PDF/View Report finding and documentation cards.
 * Shows the visible sequence number only — truncated internal record ids are never displayed.
 */
export const REPORT_CARD_NUMBER_CELL_CLASS =
  'flex w-12 shrink-0 items-center justify-center font-black text-2xl';
