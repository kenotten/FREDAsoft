/** Workspace sticky selections persisted in localStorage (App). */
export const FREDASOFT_SELECTIONS_LOCAL_STORAGE_KEY = 'fredasoft_selections';

/** Data Entry draft recovery (localStorage). */
export const FREDASOFT_DRAFT_LOCAL_STORAGE_KEY = 'fredasoft_draft';

/**
 * Data Entry Active Glossary path filter (localStorage).
 * Legacy values may be read once from sessionStorage for migration.
 */
export const ACTIVE_GLOSSARY_STORAGE_KEY = 'fredasoft_data_entry_active_glossary_set_v1';

/** sessionStorage key prefix: StandardsBrowser UI (search, tree, expand). */
export const STANDARDS_BROWSER_UI_STORAGE_PREFIX = 'fredasoft_standards_browser_ui_v1:';

/** sessionStorage key prefix: selected type/version slice in Standards Browser. */
export const STANDARDS_BROWSER_SELECTION_STORAGE_PREFIX = 'fredasoft_standards_browser_selection_v1:';
